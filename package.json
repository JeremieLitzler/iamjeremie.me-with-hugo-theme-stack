{
  "scripts": {
    "netlify": "netlify",
    "build": "hugo --gc --minify",
    "test:xml": "node scripts/validate-xml-output.mjs",
    "build:ci": "npm run build && npm run test:xml"
  },
  "dependencies": {
    "@netlify/functions": "^2.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "netlify-cli": "^17.20.1"
  }
}
